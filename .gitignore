# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output
.next/
out/

# Production build
build/

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
*.pem

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
.windsurf/

# Wiki build artifacts (generated — never commit)
public/wiki-manifest.json

# Submodule contents (tracked via .gitmodules, not by parent repo)
content/core-lore/

# Internal Soulforge drone work products (never ship)
BLACKBOARD.json
BLACKBOARD.json.lock
BLACKBOARD_*.json
BLACKBOARD*.json

# --- S298 deploy hardening (GU2 FLAG 1: comments on their OWN lines, never trailing) ---
# Finder-duplicated build dirs (".next (1)" etc.) — the plain ".next/" above misses these
.next */
# macOS no-sync build/cache variants (node_modules.nosync, .next.nosync)
*.nosync/
# internal planning docs — never to the public site repo
/DEPLOY_PLAN.md
/WEBSITE_AUDIT.md
/SITE_RESTRUCTURE_PLAN.md
/website-wireframe-v2.png
SCOUT_REPORT.json
VERIMAP.json
OBSIDIAN_REPORT.json
REFINERY_REPORT.json
POLECAT_DONE
POLECAT_RESULT.json
team_roster.md
project-brief.md
*_north_star.md
.forge-staging/

# Session/internal docs (never ship)
KINGDOM_MAP/SESSION_*.md
KINGDOM_MAP/DESIGN_PATCH_PLAN.md

# Internal scripts (never ship)
_merge_playground.py

# Public directory — internal files
public/sinner-king-home.html
public/*.backup*

# Root-level playground files (local dev copies, not the committed public/ versions)
/sinner-kingdom-playground.html

# internal SYSTEMS tracking home (North Stars/changelogs/bugs) — visible in Finder, NOT to the public repo
/_PAGES/
